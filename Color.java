import java.util.*;

public class Color {
    Random colorGenerator = new Random();
    int greenValue;
    int redValue;
    int blueValue;

    public Color(int green, int red, int blue) {
        greenValue = green;
        redValue = red;
        blueValue = blue;
    }

    public Color() {
        greenValue = colorGenerator.nextInt(256);
        redValue = colorGenerator.nextInt(256);
        blueValue = colorGenerator.nextInt(256);
    }

    public void 
    
    
}
